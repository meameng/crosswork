import '../css/contTit.scss';

const ContTit = ({title}) => {
    return(
        <div className="titArea">
            <h3 className="contTit">{title}</h3>
        </div>
    )
}

export default ContTit;